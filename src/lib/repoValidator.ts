// Compatibility export for CLI callers. Not a browser module.
export { validateRepository } from '../../server/repoValidator.ts';
export type { ValidationResult } from '../../server/repoValidator.ts';
