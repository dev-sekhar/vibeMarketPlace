/**
 * Home page display configuration.
 * Adjust these values to control how many items appear on the home page.
 */

/** Number of apps shown in the default grid (no search/filter active). */
export const HOME_APPS_LIMIT = 5;

/** Number of latest whitepaper titles shown next to the stats bar. */
export const HOME_WHITEPAPERS_LIMIT = 5;
